{
  "name": "python-flask-app",
  "version": "1.0.0",
  "description": "A Python Flask app for serving HTML files with embedded JS",
  "scripts": {
    "start": "python server.py",
    "dev": "python server.py"
  },
  "dependencies": {
    "flask": "^2.2.2"
  },
  "engines": {
    "python": ">=3.7.0"
  }
}
